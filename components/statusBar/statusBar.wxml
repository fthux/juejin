<view class="status-bar {{transparent ? 'transparent' : ''}}" style="height: {{statusHeight}}px;"></view>
<view wx:if="{{navigation}}" class="navigation-row {{transparent ? 'transparent' : ''}}" style="height: {{navigationHeight}}px; padding-right: {{menuInset}}px;">
  <slot></slot>
</view>
