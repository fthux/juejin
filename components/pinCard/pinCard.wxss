.pin-card {
  padding: 28rpx 32rpx 0;
  background: var(--jj-surface);
  border-bottom: 16rpx solid var(--jj-bg);
}

.pin-head {
  display: flex;
  align-items: center;
}

.avatar {
  width: 72rpx;
  height: 72rpx;
  border-radius: 50%;
  background: var(--jj-fill);
}

.identity {
  min-width: 0;
  margin-left: 18rpx;
  flex: 1;
}

.name {
  color: var(--jj-text);
  font-size: 28rpx;
  font-weight: 600;
}

.caption {
  margin-top: 6rpx;
  color: var(--jj-text-3);
  font-size: 23rpx;
}

.more {
  width: 46rpx;
  height: 46rpx;
  padding: 8rpx;
}

.card-follow {
  height: 50rpx;
  padding: 0 18rpx;
  display: flex;
  align-items: center;
  border: 1rpx solid var(--jj-blue);
  border-radius: 4rpx;
  color: var(--jj-blue);
  font-size: 21rpx;
}

.card-follow.followed {
  border-color: var(--jj-line);
  color: var(--jj-text-3);
}

.pin-content {
  margin: 20rpx 0 16rpx;
  color: var(--jj-text);
  font-size: 30rpx;
  line-height: 1.65;
  word-break: break-all;
}

.pin-pictures {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 8rpx;
}

.pin-picture {
  width: 100%;
  height: 210rpx;
  border-radius: 4rpx;
  background: var(--jj-fill);
}

.pin-link {
  min-height: 104rpx;
  margin-top: 16rpx;
  padding: 14rpx;
  display: flex;
  align-items: center;
  box-sizing: border-box;
  background: var(--jj-fill);
}

.link-icon {
  width: 72rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #ffffff;
  background: var(--jj-blue);
  font-size: 34rpx;
}

.link-copy {
  min-width: 0;
  margin-left: 16rpx;
  flex: 1;
}

.link-title {
  color: var(--jj-text-2);
  font-size: 23rpx;
}

.link-host {
  margin-top: 8rpx;
  color: var(--jj-text-3);
  font-size: 20rpx;
}

.topic {
  width: fit-content;
  margin-top: 18rpx;
  padding: 6rpx 12rpx;
  border-radius: 4rpx;
  color: #1e80ff;
  background: var(--jj-blue-soft);
  font-size: 25rpx;
}

.pin-actions {
  height: 88rpx;
  margin-top: 18rpx;
  display: flex;
  align-items: center;
  color: var(--jj-text-3);
  border-top: 1rpx solid var(--jj-line);
  font-size: 25rpx;
  text-align: center;
}

.pin-action {
  width: 0;
  min-width: 0;
  height: 88rpx;
  margin: 0;
  padding: 0;
  flex: 1 1 0;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  color: inherit;
  background: transparent;
  font-size: inherit;
  line-height: 1;
  box-sizing: border-box;
}

.pin-action:first-child {
  justify-content: center;
}

.pin-action:last-child {
  justify-content: center;
}

.pin-action image {
  width: 30rpx;
  height: 30rpx;
  opacity: 0.78;
}

.liked {
  color: #1e80ff;
}

.pin-card.compact {
  padding: 24rpx 28rpx 0;
  border-bottom-width: 14rpx;
  border-bottom-color: var(--jj-bg);
}

.compact .avatar {
  width: 62rpx;
  height: 62rpx;
}

.compact .name {
  font-size: 26rpx;
}

.compact .caption {
  font-size: 21rpx;
}

.compact .pin-content {
  margin: 16rpx 0;
  font-size: 27rpx;
  line-height: 1.58;
}

.compact .pin-actions,
.compact .pin-action {
  height: 72rpx;
}
