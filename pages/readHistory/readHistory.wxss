.history-head {
  height: 84rpx;
  padding: 0 32rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
  color: var(--jj-text-2);
  font-size: 25rpx;
}

.clear {
  color: var(--jj-text-3);
}
